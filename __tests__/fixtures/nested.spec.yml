name: nested-fixture
root:
  stage: root
  steps:
    - stage: outer
      expand: { count: 2, as: i }
      steps:
        - stage: "inner-{i}"
          steps:
            - leaf: "work-{i}"
              agent: claude-code
              task: "Do work for {i}"
              claims: ["out/{i}.txt"]
